#include "main.h"

/**
 *add  - the main function to print lowercase alphapet
 *@x: is avriable to check
 *@z: is aparameter
 *description: this function to print putchar
 *Return: z the result
 */
int add(int x, int z)
{

	return (x + z);
}
