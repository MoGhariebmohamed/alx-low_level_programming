#include "main.h"
/**
 *mul - for multiply
 *description: this function for digits check
 *@a: the digit for usage
 *@b: the second digit
 *Return: 1 if true and 0 if not
 */
int mul(int a, int b)
{
	long int x = a * b;
return (x);
}
