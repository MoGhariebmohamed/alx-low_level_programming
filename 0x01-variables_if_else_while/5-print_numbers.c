#include <stdio.h>
/**
 *main - the main function here
 *
 *description: the printing for charachter alpha
 *Return: 0 sucess
 */
int main(void)
{
	int no;

	for (no = 0; no < 10; no++)
	{
		putchar(no);
	}
	putchar('\n');
	return (0);
}
